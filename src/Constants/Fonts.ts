export type FontsType = {
    regular: string,
    medium: string,
    semiBold: string,
    bold: string,
};

const Fonts: FontsType = {
    regular: 'system',
    medium: 'system',
    semiBold: 'system',
    bold: 'system',
};

export default Fonts;