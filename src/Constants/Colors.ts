export interface ColorsType {
    PRIMARY: string,
    SECONDARY: string,
    ACCENT: string,
};

const Colors: ColorsType = {
    PRIMARY: 'red',
    SECONDARY: 'green',
    ACCENT: 'blue',
};

export default Colors;